export interface IUser {
    address?: string;
    createdAt: string;
    updatedAt: string;
    email: string;
    id: string;
    name: string;
    password: string;
    role?: string;
    profileImg?: string
}